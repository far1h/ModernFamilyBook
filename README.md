#  Using Models and Classes for creating TableViews in UIKit

- Added TableView to ViewController
- Set up TableView constraints to fill the entire view
- Added another ViewController to display details
- Embedded NavigationController for navigation
- Added UI elements to DetailViewController
- Set elements constraints in DetailViewController
- Set up segue from ViewController to DetailViewController
- Create a cocoa touch file for DetailViewController
- Edit attributes controller to set custom class to DetailViewController
- Add TableView delegate and data source protocols to ViewController
- Implement required TableView methods
- Set TableView delegate and data source to self in ViewDidLoad of ViewController
- Create a Model class to represent data
- Create an array of Model objects in ViewController
- Populate TableView cells with data from Model objects
- Implement didSelectRowAt method to handle row selection and perform segue 
- Prepare for segue to pass data to DetailViewController
- In DetailViewController, create a variable to hold the passed Model object
- In DetailViewController's ViewDidLoad, set UI elements with data from the passed Model

